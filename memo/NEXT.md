# 次やりたいこと

**TERRAFORM**に完全移行したい
**GCSでマウントは面倒なので、直接モデルをDOCKERイメージに含めてしまいたい**

- 使い方を記載したパネルを作成（開閉可能）
- AIに渡すシステムプロンプトを変更できるようにする
- 各処理ノードで結果しか出てない（元画像が分からない）ので、機能を消すか、あるいは元画像も表示するようにする
    - Historyの箇所でも同様なので、片方だけ機能を残して（historyの方が全体が見通しやすいのでそっち）、そっちだけで元画像も見られるようにする
- AIのツール呼び出しを描画させる
- 画像取得のツールが上手くいってなさそう
- AIチャットで画像を渡せるようにしたい