# ZENN記事の技術内容

ZENN記事でこのアプリの技術部分について説明。　
コード例などはのせず、概要・全体のつながりなどを中心に解説

## アプリ概要

Visionfy Demoは、ノードベースのビジュアル画像処理ワークフローアプリケーションです。ユーザーはキャンバス上でStart → Process → Endのノードをつなぎ、画像に対してOpenCV変換やML異常検知を順番に適用できます。処理結果はリアルタイムでプレビューでき、AI チャットアシスタントがワークフローの構築を支援します。

## アプリ構成

モダンなWeb技術と堅牢なバックエンドを組み合わせた2層構成を採用しました。フロントエンドとバックエンドはそれぞれ独立したコンテナとしてGoogle Cloud Run上で動作します。

### 全体アーキテクチャ

```
ユーザー（ブラウザ）
    │
    ▼
┌──────────────────────────┐       ┌───────────────────────┐
│  Cloud Run: Frontend     │       │  Secret Manager       │
│  Next.js 16 (port 3000)  │──────▶│  (GEMINI_API_KEY)     │
│  1 CPU / 512Mi           │       └───────────────────────┘
└──────────┬───────────────┘
           │ /api/process-node
           ▼
┌──────────────────────────┐       ┌───────────────────────┐
│  Cloud Run: Backend      │       │  Cloud Storage        │
│  Flask (port 8080)       │──────▶│  (model.ckpt)         │
│  2 CPU / 4Gi             │       └───────────────────────┘
└──────────────────────────┘
```

### Webアプリケーションレイヤー：Next.js

インタラクティブなノードエディタを実現するために、フロントエンドには**Next.js 16**を採用。React Flow（@xyflow/react）を活用したキャンバスUI、AIチャットパネル、画像アップロード・結果表示パネルの3ペイン構成で、直感的なワークフロー構築を実現しています。

### 画像処理APIレイヤー：Python / OpenCV / Flask

実際の画像処理ロジックは、ライブラリ資産が豊富な**Python**と**OpenCV**で実装。APIサーバーとして**Flask**を利用し、フロントエンドからのリクエストに対して処理結果を返します。本番環境では**Gunicorn**（1ワーカー/8スレッド）で稼働させ、GIL制約下でも安定した並行処理を実現しています。

## 画像処理パイプライン

### データフロー

1. ユーザーが画像をアップロードし、キャンバス上でワークフローノードを構成
2. 実行時、フロントエンドがノードグラフをStart → Process → Endの順に走査
3. 各Processノードについて、Next.js APIルート（`/api/process-node`）経由でFlaskバックエンドを呼び出し
4. バックエンドがOpenCV/ML変換を適用し、JPEG画像として結果を返却
5. 結果が次のノードに伝播し、Endノードで最終出力を表示

### 対応する画像処理関数（7種）

| 関数名 | 処理内容 | 主なパラメータ |
|--------|---------|---------------|
| CLAHE | コントラスト制限付き適応ヒストグラム均等化 | clipLimit, tileGridSize |
| Gaussian Blur | ガウシアンぼかし | ksize, sigmaX, sigmaY |
| Grayscale | グレースケール変換（オプションで二値化） | enableThreshold, threshold |
| Remove Noise | メディアンフィルタによるノイズ除去（3×3固定） | なし |
| Restore Brightness | 輝度補正（強度シフト） | value |
| Restore Contrast | ガンマ補正によるコントラスト復元 | gamma |
| Model Inference | Patchcore異常検知＋ヒートマップ | overlayAlpha, heatmapAlpha |

バックエンドでは**ファクトリパターン**による統一パイプラインを採用し、各関数のパラメータ解析→処理→レスポンス生成を共通化しています。パラメータは**frozen dataclass**で定義し、不変性と型安全性を確保しています。

## ML異常検知（Patchcore）

バックエンドには**anomalib**ライブラリを用いたPatchcoreモデルを統合しています。

- **モデル**: anomalib 2.2.0のPatchcore（PyTorch + Lightning）
- **推論**: CPU専用（CUDA不要、Cloud Run互換）
- **モデルファイル**: Google Cloud Storageに配置し、初回推論時に遅延ロード＋メモリキャッシュ
- **出力**: 異常マップ（ヒートマップ）＋ 異常スコア（0〜1のfloat値）

ヒートマップは`overlayAlpha`（元画像の透明度）と`heatmapAlpha`（異常マップの透明度）で合成度合いを調整できます。異常スコアはレスポンスヘッダ（`X-Anomaly-Score`）として返却され、フロントエンドで参照可能です。

## AI チャットアシスタント

### 概要

ワークフロー構築を支援するAIチャット機能を搭載しています。**Google Gemini 3 Pro Preview**をLangChain経由で統合し、ワークフローのコンテキストを理解した上でのアドバイスやワークフロー自動生成が可能です。

### アーキテクチャ

1. ユーザーがチャットパネルからメッセージ（＋オプションで画像）を送信
2. Next.js APIルート（`/api/chat`）がリクエストを受信
3. **LangChain**エージェントがGemini + ツール群を使って回答を生成
4. レスポンスをトークン単位でストリーミング配信
5. フロントエンドがMarkdownとツール実行インジケータをリアルタイム描画

### AIツール（4種）

| ツール名 | 機能 |
|---------|------|
| get_available_nodes | 利用可能な画像処理ノードの一覧と説明を取得 |
| get_workflow_context | 現在のワークフロー構成（ノード・エッジ・パラメータ）を取得 |
| generate_workflow | AIがワークフローを自動生成してキャンバスに反映 |
| get_image | ワークフロー上の画像（元画像・処理結果）を取得してGeminiに送信 |

システムプロンプトはサーバーサイドで管理され、クライアントには公開されません。マルチスレッド対応で、チャット履歴はlocalStorageに永続化されます。

## ノードベースワークフロー（React Flow）

### キャンバスUI

フロントエンドのワークフローエディタは**React Flow**（@xyflow/react）で実装しています。

- **3種のノードタイプ**: Start（画像入力）、Process（画像処理）、End（結果表示）
- **リニアパイプライン制約**: 分岐やループを禁止し、直線的な処理フローを強制
- **ドラッグ&ドロップ**: ノードの追加・接続・削除をマウス操作で直感的に実行
- **実行ステータス**: 各ノードの処理状態（待機中/実行中/完了/エラー）をリアルタイム表示
- **Before/Afterプレビュー**: ノードにホバーすると、処理前後の画像を比較表示

### 状態管理

React Contextベースの状態管理を採用しています。

- **FlowStore**: ノード、エッジ、ビューポートの中央管理
- **InspectorContext**: 画像アップロード状態と実行トリガーの共有
- **ToastContext**: グローバルな通知（エラー/警告/情報/成功）

### スナップショット機能

ワークフロー構成の保存・復元機能を実装しています。

- 最大20件のスナップショットをlocalStorageに保存
- ページロード時に最新のスナップショットを自動復元
- 保存時に実行ステータスや結果画像を除去してシリアライズ
- インポート/エクスポート（JSON形式）にも対応

## 型安全設計

### Single Source of Truth（SSOT）

画像処理関数の定義は`PROCESS_FUNCTIONS_BASE`を唯一の定義元とし、そこからデフォルトパラメータ、アイコン、AI向け説明文、UIコンフィグを自動導出する設計です。新しい画像処理関数の追加時にも、この定義を更新するだけで各所に反映されます。

### 型システム

- **判別可能ユニオン型**: `ProcessNodeData`を7つのバリアントの判別可能ユニオンとして定義
- **ランタイム型ガード**: `isProcessNodeData()`等のガード関数で実行時の型安全性を確保
- **Zodバリデーション**: AIツール入力に対するスキーマ検証

### エラーハンドリング

統一的なエラー階層（`AppError`）を定義し、ネットワークエラー・バリデーションエラー・処理エラーをノード単位で識別・帰属できる仕組みです。ユーザーには日本語メッセージを表示し、技術的な詳細はコンソールログに出力しています。

## 採用ライブラリ

### フロントエンド

| ライブラリ | バージョン | 用途 |
|-----------|-----------|------|
| Next.js | 16.1.4 | Webフレームワーク（スタンドアロン出力モード） |
| React | 19.2.3 | UIライブラリ |
| TypeScript | 5 | 型安全な開発 |
| Tailwind CSS | 4 | ユーティリティファーストCSS |
| @xyflow/react | 12.10.0 | ノードベースキャンバスUI |
| LangChain | 1.2.16 | AIエージェントフレームワーク |
| FilePond | 4.32.11 | ファイルアップロードUI |
| Zod | - | スキーマバリデーション |
| Pino | 10.3.0 | 構造化ロギング |

### バックエンド

| ライブラリ | バージョン | 用途 |
|-----------|-----------|------|
| Flask | 3.1.2 | Webフレームワーク |
| Gunicorn | 25.0.3 | 本番WSGIサーバー |
| OpenCV | 4.13.0 | 画像処理（headless版） |
| PyTorch | 2.10.0 | MLフレームワーク（CPU専用） |
| anomalib | 2.2.0 | 異常検知モデル（Patchcore） |
| PyTorch Lightning | 2.6.1 | MLトレーニングフレームワーク |
| NumPy | 2.4.2 | 数値演算 |
| scikit-image | 0.26.0 | 画像処理ユーティリティ |

## インフラ構成

### インフラ管理：Terraform

GCPリソースの管理にはすべて**Terraform**を使用し、インフラをコードとして管理しています。プロジェクト作成、API有効化、サービスアカウント、Cloud Run、Cloud Storage、Secret Managerまで一括で構成可能です。

### ホスティング環境：Google Cloud Run

フロントエンド・バックエンドともに**Google Cloud Run**上でコンテナとして動作します。

| サービス | ポート | CPU | メモリ | スケーリング |
|---------|--------|-----|--------|------------|
| Frontend (Next.js) | 3000 | 1 | 512Mi | 0〜5インスタンス |
| Backend (Flask) | 8080 | 2 | 4Gi | 0〜3インスタンス |

- 最小0インスタンス設定によりアイドル時のコストを最小化
- バックエンドのスタートアッププローブは100秒のタイムアウトを設定（PyTorchモデルの初回ロードに対応）
- 両サービスとも`allUsers`にInvokerロールを付与し、パブリックアクセスを許可

### コンテナ構成：Docker マルチステージビルド

両サービスともマルチステージビルドでイメージサイズを最適化しています。

**フロントエンド**（3ステージ）:
- deps → builder → runner の3段階
- `node:22-alpine`ベース、Next.jsスタンドアロン出力
- 非rootユーザー（`nextjs:nodejs`）で実行

**バックエンド**（2ステージ）:
- builder → runtime の2段階
- `python:3.13-slim`ベース、CPU専用PyTorchホイール
- Gunicorn: 1ワーカー / 8スレッド / タイムアウトなし（Cloud Runがリクエストライフサイクルを管理）

### シークレット管理・ストレージ

- **Secret Manager**: Gemini APIキーを安全に保管。フロントエンドのサービスアカウントのみ読み取り可能
- **Cloud Storage**: MLモデルチェックポイント（`model.ckpt`）を保管。バックエンドのサービスアカウントのみ読み取り可能

### IAMとサービスアカウント

最小権限の原則に従い、各サービスに専用のサービスアカウントを割り当てています。

| サービスアカウント | 権限 | 用途 |
|------------------|------|------|
| frontend-cloudrun | `secretmanager.secretAccessor` | Gemini APIキーの読み取り |
| backend-cloudrun | `storage.objectViewer` | モデルファイルの読み取り |

### デプロイフロー

```
1. Dockerイメージのビルド＆プッシュ（backend + frontend）
2. terraform apply（Cloud Runサービスの更新）
3. terraform output（デプロイ先URLの確認）
```

コード変更時は、新しいタイムスタンプタグでDockerイメージを再ビルドし、Terraform変数を更新して`terraform apply`を実行するだけで更新が完了します。
