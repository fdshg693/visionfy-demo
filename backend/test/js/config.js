
// Configuration for the API client
const config = {
    // Assuming the backend runs on localhost:8080.
    // Adjust this if your environment differs.
    baseURL: 'http://localhost:8080'
};

export default config;
